setuptools==69.5.1
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
python-multipart==0.0.12
httpx==0.27.0
av==17.0.1

# STT — setup.sh installs the right backend automatically:
#   Apple Silicon  → mlx-whisper
#   CPU / CUDA     → faster-whisper soundfile
#   Cloud fallback → groq  (set VOCO_GROQ_API_KEY)  or  openai  (set VOCO_OPENAI_API_KEY)

# LLM agents (optional extended mode)
crewai==0.80.0
langchain==0.3.0
langchain-community==0.3.0
chromadb==0.5.0
